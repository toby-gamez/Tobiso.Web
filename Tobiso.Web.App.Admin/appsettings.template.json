{
  "ConnectionStrings": {
    "DefaultConnection": "server=YOUR_SERVER;user=YOUR_USER;password=YOUR_PASSWORD;database=YOUR_DATABASE;Encrypt=False"
  },
  "Auth": {
    "Basic": {
      "Username": "YOUR_USERNAME",
      "PasswordHash": "YOUR_PBKDF2_PASSWORD_HASH",
      "UserId": "YOUR_USER_ID"
    },
    "Jwt": {
      "Secret": "YOUR_JWT_SECRET_AT_LEAST_32_CHARACTERS",
      "Issuer": "tobiso",
      "Audience": "tobiso"
    }
  },
  "Api": {
    "BaseAddress": "https://www.yourdomain.com",
    "FilesBaseAddress": "https://files.yourdomain.com"
  },
  "Files": {
    "BasicAuth": {
      "_comment": "Must match Auth:Basic:Username/Password configured on the Tobiso.Web.Files deployment - it authenticates with HTTP Basic auth, not a JWT.",
      "Username": "YOUR_FILES_USERNAME",
      "Password": "YOUR_FILES_PASSWORD"
    }
  },
  "Proxy": {
    "_comment": "Leave both arrays EMPTY for this deployment: production hosts on IIS with hostingModel=inprocess (see web.config), so IIS itself terminates the connection and HttpContext.Connection.RemoteIpAddress already sees the real client IP - there is no separate reverse-proxy network hop to unwrap. Only populate these if a CDN/load balancer/WAF is ever placed in front of IIS (e.g. Cloudflare) - in that case use that provider's published edge IP ranges here, never a value the deploying operator has to guess.",
    "KnownProxies": [],
    "KnownNetworks": []
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "System": "Warning"
      }
    },
    "Enrich": [ "FromLogContext" ],
    "WriteTo": [
      { "Name": "Console" },
      {
        "Name": "File",
        "Args": {
          "path": "logs/yourapp.admin.log",
          "rollingInterval": "Day",
          "shared": true
        }
      }
    ]
  }
}