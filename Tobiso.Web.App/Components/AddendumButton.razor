@using Tobiso.Web.Shared.DTOs
@using Tobiso.Web.Shared.Interfaces
@using Tobiso.Web.App.Services
@inject ITobisoWebApi Api
@inject AddendumModalService ModalService

@code {
    [Parameter]
    public int AddendumId { get; set; }

    protected override void OnInitialized()
    {
        // No-op: the clickable element in the rendered HTML triggers a JS function
        // that calls into the static bridge on PostDetail which fires the modal service.
    }
}
